[workspace]
members = [
    "yukari-movegen",
    "yukari"
]
resolver = "2"

# [profile.dev]
# opt-level = 1

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
debug = true
